/// To store all the different webs, instead of creating that for each holder.
GLOBAL_LIST_EMPTY(web_pick_list)
/// To store all of the different base web icons, instead of creating them every time the set_click_ability() proc is called.
GLOBAL_LIST_EMPTY(web_base_picks_icon_list)
/// To store all of the different web icons, instead of creating them every time the weave() proc is called.
GLOBAL_LIST_EMPTY(web_all_picks_icon_list)
